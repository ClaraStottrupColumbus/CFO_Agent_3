# requirements.txt — Python dependencies for the CFO Budgeting Agent.
# No HTTP/scraping packages: web ingestion runs through Anthropic's
# server-side web_search / web_fetch tools, so there is nothing to install.
anthropic>=0.116.0
fastapi>=0.139.0
uvicorn[standard]>=0.51.0
pandas>=2.2.0
pyarrow>=17.0.0
numpy>=1.26.0

# Excel export (app/export.py). Imported lazily inside workbook(), so an
# install that predates this line loses one route rather than the whole app.
openpyxl>=3.1.0

# Dev / tests
pytest>=8.0
