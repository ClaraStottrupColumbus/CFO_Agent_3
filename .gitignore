.venv/
__pycache__/
*.pyc
.env
.DS_Store
.pytest_cache/

# Runtime session memory (chat conversations + generated reports), not source.
data/history/

# Runtime state for the autonomous features, not source.
data/tasks.json
data/alerts.json
data/alerts_log.jsonl
data/rules.json
data/settings.json
data/scenarios.json
data/company_profile.json
data/budget_plan.json

# Generated demo datasets — rebuilt deterministically by app/generate_data.py.
data/*.parquet
data/*.csv

# Files the CFO added on the Data ingestion page: the registry, the stored
# datasets/documents and the converted-markdown cache. Their data, not source.
data/user_uploads.json
data/uploads/
